name: Build Android APK
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y pkg-config libssl-dev openjdk-11-jdk
        
    - name: Setup Android SDK manually
      run: |
        wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
        mkdir -p android-sdk/cmdline-tools
        unzip -q commandlinetools-linux-9477386_latest.zip -d android-sdk/cmdline-tools
        mv android-sdk/cmdline-tools/cmdline-tools android-sdk/cmdline-tools/latest
        echo "ANDROID_HOME=$PWD/android-sdk" >> $GITHUB_ENV
        echo "ANDROID_SDK_ROOT=$PWD/android-sdk" >> $GITHUB_ENV
        
    - name: Install Android Platform 30
      run: |
        yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --sdk_root=$ANDROID_HOME "platforms;android-30" "build-tools;30.0.3" "platform-tools" || true
        
    - name: Setup Rust
      run: |
        rustup target add aarch64-linux-android
        cargo install cargo-apk --version 0.9.7
        
    - name: Create debug keystore
      run: |
        cd phira
        keytool -genkey -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"
        
    - name: Add signing config to Cargo.toml
      run: |
        cd phira
        echo '' >> Cargo.toml
        echo '[package.metadata.android.signing.release]' >> Cargo.toml
        echo 'path = "debug.keystore"' >> Cargo.toml
        echo 'keystore_password = "android"' >> Cargo.toml
        echo 'key_alias = "androiddebugkey"' >> Cargo.toml
        echo 'key_password = "android"' >> Cargo.toml
        
    - name: Build APK
      run: |
        cd phira
        export ANDROID_SDK_ROOT=$ANDROID_HOME
        cargo apk build --release
        
    - name: Find and list APK files
      run: |
        find . -name "*.apk" -type f | head -20
        echo "=== Checking common paths ==="
        ls -la phira/target/ 2>/dev/null || echo "No phira/target/"
        ls -la target/ 2>/dev/null || echo "No target/"
        ls -la . 2>/dev/null || echo "No current dir"
        
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: phira-mod-apk
        path: "**/*.apk"
        retention-days: 7
